//CUSTOM hook to chekc previous state

// function usePrevious(value) {
//   const ref = useRef();
//   useEffect(() => {
//     ref.current = value;
//   });
//   return ref.current;
// }
// const { search } = props;
// const prevSearch = usePrevious({ search });

// first argument is what to do, second is when to do it
useEffect(() => {});
